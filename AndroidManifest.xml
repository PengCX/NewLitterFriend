<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newlitterfriend"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="11" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="com.example.newlitterfriend.control.acitivity.launcher.SplashFragmentActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 展示页面 -->
        <activity android:name="com.example.newlitterfriend.control.acitivity.launcher.ExhibitionFragmentActivity" >
        </activity>
        <!-- 登陆页面 -->
        <activity android:name="com.example.newlitterfriend.control.acitivity.login.LoginFragmentActivity" >
        </activity>
        <!-- 注册页面 -->
        <activity android:name="com.example.newlitterfriend.control.acitivity.login.RegisterFragmentActivity" >
        </activity>
    </application>

</manifest>